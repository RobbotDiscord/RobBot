{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "tsconfig/packagesconfig.json",
    "compilerOptions": {
        "outDir": "dist"
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "dist"]
}