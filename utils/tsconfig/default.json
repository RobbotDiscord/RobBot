{
    "$schema": "https://json.schemastore.org/tsconfig",
    "extends": "tsconfig/baseconfig.json",
    "compilerOptions": {
        "outDir": "dist"
    },
    "include": ["src/**/*.ts"],
    "exclude": ["node_modules", "dist"]
}